@page
@model Project334.Pages.Vaccines.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vaccine[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vaccine[0].Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vaccine[0].ProductionDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Vaccine) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductionDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.VaccineID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.VaccineID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.VaccineID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
